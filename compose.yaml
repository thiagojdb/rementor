services:
  demo:
    build: .
    ports:
      - "${REMENTOR_DEMO_HOST:-127.0.0.1}:8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 2s
      timeout: 1s
      retries: 20
